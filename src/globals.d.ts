/// <reference types="vite/client" />

/**
 * Compile-time constant injected by Vite's `define` option.
 * Source: `vite.config.ts` — reads `git rev-parse --short HEAD`.
 */
declare const __BUILD_HASH__: string;
